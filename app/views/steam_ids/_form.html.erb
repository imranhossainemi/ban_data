<%= form_with(model: steam_id) do |form| %>
  <% if steam_id.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(steam_id.errors.count, "error") %> prohibited this steam_id from being saved:</h2>

      <ul>
        <% steam_id.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :steam_name, style: "display: block" %>
    <%= form.text_field :steam_name %>
  </div>

  <div>
    <%= form.label :steam_num, style: "display: block" %>
    <%= form.number_field :steam_num %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
